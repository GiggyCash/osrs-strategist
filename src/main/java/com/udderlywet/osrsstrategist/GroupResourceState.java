package com.udderlywet.osrsstrategist;

/** What fresh, enabled shared-resource evidence actually proves. */
public enum GroupResourceState
{
    NOT_A_GROUP_ACCOUNT,
    GROUP_STORAGE_DISABLED,
    GROUP_STORAGE_UNKNOWN,
    SHARED_STOCK_NONE,
    SHARED_STOCK_PARTIAL,
    SHARED_STOCK_SATISFIES_NEED
}
