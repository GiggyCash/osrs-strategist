package com.udderlywet.osrsstrategist;

/** Ordered outcome from the conservative UIM inventory-resolution policy. */
public enum UimInventoryResolutionKind
{
    USE_AS_IS,
    USE_LOW_FOOTPRINT_ALTERNATIVE,
    PRODUCTIVELY_CONSUME_RESOURCES,
    USE_VERIFIED_SAFE_STORAGE,
    BUILD_HIGH_VALUE_SAFE_STORAGE,
    USE_RESTRICTED_RETRIEVAL,
    USE_DANGEROUS_DEATH_STORAGE,
    UNRESOLVED
}
